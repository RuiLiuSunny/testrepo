name: "Hello Action"
description: "A simple custom GitHub Action example"

inputs:
  name:
    description: "Name of the user"
    required: true
    default: "World"

runs:
  using: "composite"
  steps:
    - name: Say hello
      shell: bash
      run: echo "Hello, ${{ inputs.name }}!"